# Packaging Assets

This folder is reserved for Electron packaging assets such as entitlements, icons, and signing support files.

Current contents:

- `entitlements.mac.plist`

If we add signed releases later, this is where the macOS packaging inputs should live.
